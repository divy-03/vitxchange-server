# Backend (Ts):

- `tsc --init` to initialize typescript
- `npm init` to create package.json
- `app = express();` install express
- `app.listen(PORT, ()=>{})` start the server

  Create Models, Controllers, Routes folders

- `credentials: true` use this in cors options and client side api options, to enable cookie to store in browser
